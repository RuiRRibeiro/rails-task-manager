<h1>My Tasks</h1>
<%= link_to "Create a task", new_task_path %>
<ul>
  <% @tasks.each do |task| %>
  <li>
    <h2><%= task.title %></h2>
    <p><%= task.details %> </p>
    <p><%= task.completed %> </p>
    <%= link_to "View your task", task_path(task) %>
  </li>
  <% end %>
</ul>
